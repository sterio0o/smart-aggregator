rootProject.name = 'smart-aggregator'

def backendModules = [
        'collector-service',
        'common'
]

backendModules.each { moduleName ->
    include moduleName
    project(":$moduleName").projectDir = new File(settingsDir, "$moduleName")
}