spring:
  cloud:
    gateway:
      routes:
        # Collector Service
        - id: collector-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/contents/**        # ???? ?????? ?????????? ?
          filters:
            - StripPrefix=0

server:
  port: 8080